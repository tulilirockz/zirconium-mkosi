#!/bin/bash
set -xeuo pipefail

rm -rf "$BUILDROOT/usr/etc" # ostree hates this :)
rm -rf "$BUILDROOT/usr/share/doc/niri" # both of these are required for ostree not blowing up
rm -rf "$BUILDROOT/usr/share/doc/just"
